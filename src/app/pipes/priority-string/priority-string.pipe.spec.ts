import { PriorityStringPipe } from './priority-string.pipe';

describe('PriorityStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityStringPipe();
    expect(pipe).toBeTruthy();
  });
});
